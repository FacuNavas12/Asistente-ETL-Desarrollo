# Normaliza EOL a LF en el repo, sin importar core.autocrlf local (H-O0-1).
# Evita que un working tree en CRLF reescriba archivos enteros en el diff.
* text=auto eol=lf

*.zip binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.db binary
