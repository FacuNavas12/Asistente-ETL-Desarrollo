# Copiar este archivo como .env y completar los valores
# NUNCA commitear el archivo .env

# ── Proveedor LLM ─────────────────────────────────────────────────────────────
# Cambiá solo esta variable para switchear de proveedor. No hay otro cambio necesario.
# Opciones: "gemini" | "anthropic"
LLM_PROVIDER=gemini

# ── Google Gemini ──────────────────────────────────────────────────────────────
GOOGLE_API_KEY=AIza...
# Modelo para generación ETL e inferencia (también usado para validación/documentación).
# Opciones sugeridas: gemini-2.5-flash | gemini-2.5-pro
GEMINI_MODEL=gemini-2.5-flash

# ── Anthropic Claude ───────────────────────────────────────────────────────────
# Solo requerido cuando LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...
# Opciones sugeridas: claude-sonnet-4-20250514 | claude-opus-4-20250514
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# ── Base de datos de la aplicación ────────────────────────────────────────────
# Dev local: SQLite (sin instalacion, archivo app.db en backend/). Default si se omite.
# Prod: postgresql+psycopg://user:pass@localhost:5432/etl_db (driver: psycopg v3)
DATABASE_URL=sqlite:///./app.db

# ── Cifrado de credenciales ───────────────────────────────────────────────────
# Claves Fernet para cifrado de passwords de conexiones (lista separada por comas).
# La PRIMERA clave es la activa para encriptar; las siguientes solo desencriptan durante rotación.
# Generar una clave: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Ejemplo con rotación en curso: CREDENTIALS_ENCRYPTION_KEYS=NUEVA_CLAVE,CLAVE_ANTERIOR
# NUNCA commitear estas claves. Sin ellas los passwords cifrados son irrecuperables.
CREDENTIALS_ENCRYPTION_KEYS=
