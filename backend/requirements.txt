# requirements.txt — dependencias de producción del backend ETL
# Versiones fijadas desde el venv activo (Python 3.14.5 / Windows)
# Para reproducir: pip install -r requirements.txt

# ─── Framework ───────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.47.0
python-multipart==0.0.20   # requerido por FastAPI para Form data y file uploads
starlette==1.0.0           # transitive de fastapi
annotated-doc==0.0.4       # transitive de fastapi
click==8.4.0               # transitive de uvicorn
colorama==0.4.6            # transitive de uvicorn (colores en terminal Windows)
websockets==16.0           # transitive de uvicorn y google-genai

# ─── Modelos de IA ───────────────────────────────────────────────────────────
# "from google import genai" → paquete google-genai (no google-cloud-*)
google-genai==2.4.0
google-auth==2.53.0        # transitive de google-genai
pyasn1==0.6.3              # transitive de google-auth
pyasn1_modules==0.4.2      # transitive de google-auth
tenacity==9.1.4            # transitive de google-genai (reintentos)
distro==1.9.0              # transitive de google-genai (detección de OS)
# "import anthropic" → SDK oficial Anthropic; usado cuando LLM_PROVIDER=anthropic
anthropic==0.105.2
docstring_parser==0.18.0   # transitive de anthropic
jiter==0.15.0              # transitive de anthropic (JSON streaming)

# ─── Bases de datos ──────────────────────────────────────────────────────────
# "from sqlalchemy import ..." → paquete SQLAlchemy (S mayúscula en PyPI)
SQLAlchemy==2.0.49
greenlet==3.5.0            # transitive de SQLAlchemy (bridge sync/async)
alembic==1.18.4
Mako==1.3.12               # transitive de alembic (templates de migraciones)
MarkupSafe==3.0.3          # transitive de Mako / Jinja2

# Driver PostgreSQL — requerido para URLs postgresql+psycopg://
# psycopg v3 (psycopg3); paquete distinto a psycopg2
psycopg==3.3.4
psycopg-binary==3.3.4      # extensión binaria C de psycopg3 (obligatoria en Windows)
tzdata==2026.2             # transitive de psycopg (base de datos de zonas horarias)

# Driver SQL Server — requerido para URLs mssql+pyodbc://
# Requiere ODBC Driver instalado en el sistema (ej. "ODBC Driver 17/18 for SQL Server")
pyodbc==5.3.0

# ─── Validación y configuración ──────────────────────────────────────────────
pydantic==2.13.4
# "from pydantic_settings import BaseSettings" → paquete pydantic-settings
pydantic-settings==2.14.1
pydantic_core==2.46.4      # transitive de pydantic (núcleo compilado en Rust)
annotated-types==0.7.0     # transitive de pydantic
typing-inspection==0.4.2   # transitive de pydantic
typing_extensions==4.15.0  # transitive compartido (pydantic, fastapi, sqlalchemy…)
python-dotenv==1.2.2       # transitive de pydantic-settings; carga backend/.env

# ─── Seguridad / Cifrado ─────────────────────────────────────────────────────
# "from cryptography.fernet import Fernet" → paquete cryptography
cryptography==48.0.0
cffi==2.0.0                # transitive de cryptography (bindings C)
pycparser==3.0             # transitive de cffi
# Validación de JWT para autenticación de la API (AUTH_REQUIRED=true en producción)
# Usa el backend "cryptography" ya instalado para RS256; no requiere deps adicionales.
PyJWT==2.10.1

# ─── HTTP / Networking ───────────────────────────────────────────────────────
httpx==0.28.1              # transitive de google-genai (HTTP transport)
httpcore==1.0.9            # transitive de httpx
h11==0.16.0                # transitive de httpcore (parser HTTP/1.1)
anyio==4.13.0              # transitive de fastapi y google-genai (I/O asíncrono)
sniffio==1.3.1             # transitive de anyio
requests==2.34.2           # transitive de google-genai y frictionless (HTTP)
certifi==2026.4.22         # transitive de httpx/requests (certificados TLS)
idna==3.15                 # transitive de httpx/requests (nombres de dominio)
urllib3==2.7.0             # transitive de requests
charset-normalizer==3.4.7  # transitive de requests

# ─── Extracción de esquema de archivos (Frictionless v5) ─────────────────────
# Usado en POST /api/schema/infer para inferir tipos desde CSV y Excel.
# Detector(sample_size=n) — parámetro verificado en v5.19.0, mismo nombre que v4.
frictionless[excel]==5.19.0
# — transitivas de frictionless core —
attrs==26.1.0              # validación interna
chardet==7.4.3             # detección de encoding (UTF-8, Windows-1252, Latin-1…)
humanize==4.15.0           # formateo de tamaños/fechas en mensajes de error
isodate==0.7.2             # parse de duraciones ISO 8601
Jinja2==3.1.6              # templates en reports de validación
jsonschema==4.26.0         # validación de Table Schema
jsonschema-specifications==2025.9.1  # transitive de jsonschema
markdown-it-py==4.2.0     # render de markdown en reports
marko==2.2.3               # parser de markdown
mdurl==0.1.2               # transitive de markdown-it-py
petl==1.7.19               # backend de transformación tabular
python-dateutil==2.9.0.post0  # parse flexible de fechas
python-slugify==8.0.4      # normalización de nombres de archivo
PyYAML==6.0.3              # lectura de descriptores .yaml
referencing==0.37.0        # transitive de jsonschema
rfc3986==2.0.0             # validación de URIs en descriptores
rich==15.0.0               # output enriquecido en CLI de frictionless
rpds-py==2026.5.1          # transitive de referencing (estructuras inmutables Rust)
shellingham==1.5.4         # transitive de typer (detección de shell)
simpleeval==1.0.7          # evaluación segura de expresiones en reglas
six==1.17.0                # compatibilidad Python 2/3 (dep legacy de algunas libs)
tableschema-to-template==0.0.13  # generación de plantillas desde Table Schema
tabulate==0.10.0           # formato tabular en output de frictionless
text-unidecode==1.3        # transitive de python-slugify
typer==0.26.6              # CLI de frictionless
validators==0.35.0         # validación de emails, URLs, etc. en profiling
# — transitivas de frictionless[excel] —
et_xmlfile==2.0.0          # transitive de openpyxl
openpyxl==3.1.5            # lectura/escritura de .xlsx
xlrd==2.0.2                # lectura de .xls (formato legacy Excel 97-2003)
xlsxwriter==3.2.9          # escritura de .xlsx (alternativa a openpyxl)
xlwt==1.3.0                # escritura de .xls (formato legacy)

# ─── Extracción de esquema desde DDL (sqlglot) ───────────────────────────────
# Usado en POST /api/schema/from-ddl para parsear CREATE TABLE DDL.
# Soporta postgres, tsql, mysql y ANSI. Sin dependencias transitivas.
sqlglot==30.8.0

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest==9.0.3
iniconfig==2.3.0           # transitive de pytest
packaging==26.2            # transitive de pytest
pluggy==1.6.0              # transitive de pytest
pygments==2.20.0           # transitive de pytest (syntax highlighting en output)
