# Registro de errores

**Mutable en las columnas `Estado` y `Objetivo`; el resto de cada fila es append-only.** Lo escribe cualquier sesión que encuentre o cierre un error.

**Para qué existe:** para que "arreglo un error y aparece otro" sea visible en vez de vivir en la memoria de quien está trabajando. La columna que hace el trabajo es **`Origen`** — de qué error salió éste. Con eso, a cuatro niveles de profundidad se puede ver dónde se está parado.

> Se mueve a `registro/errores.md` cuando la reorganización de carpetas se ejecute. Hoy vive acá para no dejar dos estructuras a medio migrar.

---

## Las tres reglas

1. **Todo error se registra cuando se ve, aunque no se toque.** Registrar es gratis. Actuar necesita objetivo.
2. **`Origen` es obligatorio si apareció arreglando otro.** Si salió de una sesión de exploración o de una corrida, va vacío.
3. **Profundidad máxima 2.** Si E-01 destapa E-02 y E-02 destapa E-03, E-03 se registra y se deja: se vuelve a cerrar E-01. Sin tope, la cadena no termina.

**Cuándo se trabaja un error:** solo si está en el objetivo en curso, **o** si bloquea algo que sí lo está. Todo lo demás espera, registrado.

**Vocabulario de `Estado`:** `abierto` · `en curso` · `cerrado` · `congelado`.

---

## Tabla

| ID | Síntoma | Origen | Estado | Objetivo | Bloquea a |
|---|---|---|---|---|---|
| E-01 | `KtrBuilderError` en `Cargar dim_producto`: `type='Update'` fuera del vocabulario de modo N. No se entrega ningún archivo | — | cerrado (D64) | O1 | — |
| E-02 | `VALUE_META_TYPE_NAMES` se mergeó con la nota "PENDIENTE verificar contra `ValueMetaFactory.java`" sin verificar. Si la lista está incompleta, el emisor rechaza valores legítimos | E-01 | cerrado (D60) | O1 | E-01 |
| E-03 | ~~El repair de la serie fk-categoria acepta un `stream_field` que no existe en el stream y degrada a `tipo="info"`~~ — **descartado tras verificar (D60):** el test rojo (`test_repair_dimension_loader_fields_floor_when_gate_fails`) reusaba el fixture de su vecino exitoso (mismo atributo faltante, resoluble por `_deterministic_field_mapping` sin pasar nunca por el LLM fake que simulaba la alucinación) — defecto del fixture, no de `_repair_dimension_loader_fields`. El gate real (`etl_generator.py:805-815`) sí rechaza un `stream_field` inexistente, verificado con un fixture que fuerza el camino correcto. Corregido en el mismo turno | E-01 | cerrado (D60) | O1 | E-01 |
| E-16 | `_synthesize_dimension_lookup_config` (`dimension_step_policy.py:243-244`), rama `upstream_lower is None` (grafo no resoluble): asume `stream_field = attr` por identidad **sin verificar contra ningún inventario y sin finding** — mismo patrón que el criterio de D60 prohíbe, encontrado al verificar E-03 pero fuera de su alcance puntual. **Acotado, no cerrado, por D68 (O3, 2026-08-04):** la función se reescribió (`build_dimension_lookup_config`/`_resolve_stream_field`) preservando este mismo fallback para el caso general — un loader legítimo con grafo no resoluble sigue confiando en identidad sin finding, sin cambio. Se agregó un finding `warning` SOLO para la intersección con D58 (rol forzado de fact_lookup a loader + grafo no resoluble, donde además no hay ninguna otra señal de que el step sea el loader real) — ver D68. El patrón general que este hallazgo describe sigue abierto | E-03 | abierto | — | — |
| E-04 | `StringOperations` escribe índices numéricos donde Kettle exige palabras literales. El step **nunca recorta ni cambia mayúsculas**, con cualquier configuración | — | cerrado (D59) | O1 | — |
| E-05 | `Unique` escribe `case_sensitive`; el tag real es `case_insensitive`, con polaridad invertida. Ausente → Kettle compara sin distinguir mayúsculas **siempre** | — | cerrado (D59) | O1 | — |
| E-06 | `ExcelInput` nunca emite `spreadsheet_type` → Kettle cae al motor legado JXL, que **no lee `.xlsx`** | — | cerrado (D59) | O1 | — |
| E-07 | `JsonInput` nunca emite `includeNulls` → el comportamiento ante `null` lo define el `kettle.properties` de cada máquina. No-determinismo entre entornos | — | cerrado (D59) | O1 | — |
| E-08 | `TextFileOutput` anida `create_parent_folder` en `<file>`; Kettle lo lee como hijo directo de `<step>`. Inocuo hoy solo porque el único valor emitido coincide con el default | — | cerrado (D59) | O1 | — |
| E-09 | `CombinationLookup` nunca emite el bloque `<fields><return>`: surrogate key sin nombre real y autoincrement forzado a `true` | — | cerrado (O1-c) | O1 | — |
| E-10 | `DataValidator` invierte `name`/`fieldname`: Kettle busca un campo del stream con el nombre de la **etiqueta** de la regla. La validación queda rota siempre que etiqueta ≠ campo | — | cerrado (O1-c) | O1 | — |
| E-11 | `SplitFieldToRows` sin el "3" no es un plugin id registrado. Si el alias resuelve ahí, Spoon marca el step "missing" y la transformación no corre | — | cerrado (O1-c) | O1 | — |
| E-12 | `GroupBy` clásico con `all_rows="N"` fijo exige input pre-ordenado; sin esa garantía en el grafo, el agregado puede ser silenciosamente incorrecto | — | congelado | — | — |
| E-13 | `validators/__init__.py` importa dos módulos untracked → `ImportError` en cualquier clon | — | cerrado (H-O0-2, commit `6d021f4`) | O0 | todo |
| E-14 | 249 de 271 archivos "modificados" son churn CRLF. El estado real no se puede leer desde git | — | cerrado (H-O0-1, commit `4600fbb`) | O0 | O1, O2 |
| E-15 | `ESTADO.md` afirmó que 5 ítems de D55 no estaban ejecutados cuando lo estaban. Una sesión clasificó mal por leerlo | — | cerrado (H-O0-3, commit `b1a0abe`) | O0 | — |
| E-17 | I5 (multi-moneda), I7 (orden de `CREATE TABLE`) e I9 (clave degenerada del fact) se declaran como invariante en `prompt_validacion_src.txt` pero ningún `V1-V6` los chequea, y no hay validador Python que los toque (`grep moneda_origen/tasa_cambio` en `backend/app/` no devuelve nada) | — | abierto | — | — |
| E-18 | `system_inference.txt:89` (línea de PRECEDENCIA) dice "Invariantes I1-I7", pero el propio archivo declara I1-I9 (línea 74-82) — no se sabe si I8/I9 se excluyeron del nivel de máxima precedencia a propósito o es texto desactualizado | — | abierto | — | — |
| E-19 | `validators/dimension_lookup_fields.py::check_dimension_lookup_fields` comparaba `field_type.lower()` (con `.strip()`) contra `valid_vocab` — Kettle compara `<field><update>` con `equalsIgnoreCase()`, que NO recorta whitespace (`' Insert'` es tan inválido como `'SCD1'`). El validador aceptaba como válido un literal que Kettle rechaza en runtime. Encontrado auditando el dedupe del Bloque 1 (D60); ya corregido en el mismo commit que introdujo el chequeo por-modo (`24dd9af`/`392a0f9`, ver D63) a `str(raw_field_type).lower()` sin strip — se registra igual por regla 1 (todo error se registra cuando se ve) | E-01 | cerrado (D63, ya corregido al encontrarlo) | O1 | — |
| E-20 | Todo finding de `PRE_EMIT_PASSES` insensible a recuperación de tabla (`check_dimension_lookup_fields` confirmado, probablemente más) sale **duplicado** en `validaciones`/`advertencias_buenas_practicas`: `_recover_table_keys()` (H29) corre `run_passes()` completo sobre el `ktr_data` sin partir, y `build_ktr()` lo vuelve a correr completo sobre el sub-dict ya partido — mismo finding, dos veces, byte a byte. Confirmado con stack trace en la corrida real de E-01 (D64): 6 findings de vocabulario cruzado aparecieron 12 veces. Aplica a toda corrida con dimensiones (sync, async, `build_etl_from_raw`), no solo a este corpus. **Cerrado — dedupe en `_split_integrity_warnings()` (`etl_generator.py`), punto donde convergen las dos pasadas (líneas ~1099 y ~1237) antes de clasificar por prefijo: se descarta el string exacto repetido en vez de tocar los call sites de `run_passes()`. Test: `test_split_integrity_warnings_dedupe.py`** | E-01 | cerrado | O1 | — |
| E-21 | `_repair_dimension_loader_fields()` corta con `if llm is None: return` (`etl_generator.py:700-702`) ANTES de intentar `_deterministic_field_mapping()` — que no llama al LLM y hubiera resuelto el caso real (`nombre_categoria`→`categoria` por prefijo) igual. Con `llm=None` (build-from-raw sin proveedor, o cuota agotada) se pierde un repair gratis que no depende de nada externo. Encontrado corriendo la corrida real de D64. **Cerrado por construcción (D68, O3, 2026-08-04)** — no por un fix del guard: `_repair_dimension_loader_fields()` se borró entera. Su lógica determinista (prefijo `nombre_`) pasó a ser el paso 3 de la escalera de `build_dimension_lookup_config`, en el camino principal, sin ningún parámetro `llm` — no hay guard que cortar porque no hay LLM en ese camino | E-01 | cerrado (D68) | O3 | — |
| E-22 | `build_etl_from_raw()` (usado por "Reutilizar respuesta"/retry, `ai.py::build_from_raw`) nunca pasaba `dwh_ddl`/`stg_ddl` a `_build_response_from_two_ktr_data()`/`_build_response_from_data()` — a diferencia del flujo de generación normal (`generate_etl_from_inference`/`generate_etl_async`, cableado en `ffc857b`). `ETLGenerateResponse.dwh_ddl/stg_ddl` quedaban `None`, `DdlSection` (`ResultView.jsx:39`) oculta la sección entera sin ellos — reportado por el usuario ("antes se mostraba el DDL... ahora no lo vi"), reproducido con "Reutilizar respuesta". **Cerrado** — `stg_ddl`/`dwh_ddl` agregados como parámetros opcionales de punta a punta: `BuildFromRawRequest` (`etl_schemas.py`) → `ai.py::build_from_raw` → `build_etl_from_raw()` → las dos funciones de armado de respuesta → frontend (`buildFromRaw()` en `etlService.js`, ya tenía `inferResult.stg_ddl/dwh_ddl` en memoria, solo faltaba reenviarlos) en `CreateETL.jsx` (`handleFinalizeConnections`, `handleReuseResponse`). Tests: `test_etl_generate_response_shape.py` (3 nuevos) | — | cerrado | — | — |
| E-23 | Impacto de E-21 confirmado empíricamente (no solo por lectura de código): corriendo el corpus real de E-01 a través del flujo async completo (`/generate-async`→`/connections`→`/status`, no `build_etl_from_raw()` directo) con un LLM disponible, `_deterministic_field_mapping()` resolvió `nombre_categoria`→`categoria` (sinónimo por prefijo) **sin consultar al LLM** — el contador de llamadas reales quedó en 3 (auditoría DDL + 2 etapas), ninguna de repair. Confirma que mover el intento determinístico ANTES de `if llm is None: return` (E-21) resolvería el mismo caso también en el camino `build-from-raw`/retry sin proveedor (cuota agotada, o histórico con `llm=None`) — hoy ese camino pierde un repair gratis que no depende de nada externo. Test: `test_e01_corpus_through_real_async_pipeline_reaches_built` (`test_ktr_build_job_api.py`). **Cerrado junto con E-21 (D68, O3, 2026-08-04)** — reverificado corriendo `apply_dimension_contracts()` en aislamiento contra el mismo corpus: `nombre_categoria`→`categoria` resuelve en la pasada principal, cero llamadas al LLM (no hay parámetro `llm` en esa función). No reverificado a través de `/generate-async` end-to-end tras el cambio (criterio 5 de `30-decision-python-llm.md`, pendiente) | E-21 | cerrado (D68) | O3 | — |
| E-24 | El repair H51 (`dimension_step_policy.py`, vía `_repair_dimension_loader_fields`) reclasifica un step de `update=N` a `update=Y` y regenera `fields[]` con vocabulario correcto — confirmado, el `.ktr` final queda bien formado y correcto. Pero los findings `error` de `check_dimension_lookup_fields` (`validators/dimension_lookup_fields.py`) que corrieron ANTES del repair, contra el step todavía en `modo N` con el vocabulario viejo (parcialmente inventado), quedan en `result.validaciones` sin invalidarse tras la reclasificación — el usuario ve N errores "modo N, vocabulario cruzado" que ya no describen el archivo entregado (que quedó en modo Y, con el vocabulario regenerado, correcto). No es el riesgo de "segundo frente" de silenciar un problema real (acá el archivo terminó bien) sino el inverso: findings obsoletos que sobreviven al repair y podrían hacer descartar un archivo que en realidad está arreglado. Encontrado verificando los criterios 3/4 de O1-b (`10-estabilizar-emision.md`) con el corpus real de E-01 a través de `/generate-async`→`/status`. **Cerrado por eliminación del mecanismo (D68, O3, 2026-08-04)** — no hubo fix puntual: el patrón de dos pasadas (`enforce_dimension_step_policy` corre, reclasifica, corre de nuevo con `repaired_mappings`) que producía el finding obsoleto no existe más. `apply_dimension_contracts()` corre una sola vez; `VERIFY_PASSES` corre después de la síntesis completa, nunca antes — no hay ventana donde un finding describa un config que todavía va a cambiar | — | cerrado (D68) | O3 | — |
| E-25 | `routers/schema.py::infer_schema` manejaba el ciclo de vida completo del upload (tempfile, límite de 50 MB, cleanup) dentro del router — viola R2 (`arquitectura-objetivo.md`: "el router no toca... el disco"). No estaba en ningún `FROZEN_*` de `test_architecture_layers.py` porque ese test solo cubre R1/R3/R4, nunca midió R2 — hueco del radar, no deuda ya registrada. Encontrado auditando `routers/` contra la doctrina de capas (sesión de validación post-cierre de O2), sin relación con ningún crash | — | cerrado (O2-d) | O2 | — |

---

## Dónde vive la evidencia

- **E-01** — `diagnostico-fk-categoria-loader-faltante.md`; cierre en D64 (`02-decisiones.md`), corrida real contra el corpus original
- **E-20, E-21** — D64 (`02-decisiones.md`), stack trace y corrida real que cerró E-01
- **E-02, E-03, E-16** — D60 (`02-decisiones.md`); verificación hecha en la sesión de O1-b contra `pentaho-kettle` (E-02) y `tests/test_dimension_field_repair.py` (E-03/E-16)
- **E-04 … E-12** — `investigacion-tags-validos-por-step.md` § A, con clase y línea de `pentaho-kettle`
- **E-09, E-10, E-11 (cierre)** — 3 commits (`7b0a78f`, `9a15593`, `e53387e`), 3 tests nuevos en `test_build_ktr_emission.py` (uno por step, contra salida real de `build_ktr()`). Suite completa post-fix: 697 passed / 54 failed — uno menos que el corte de O1-a (55), sin regresión nueva (verificado contra `git stash`: la única falla que cambió de posición, `test_ktr_xml_validator.py::test_build_ktr_get_system_info_without_fields_gets_default_field`, falla igual sin estos commits — preexistente, no relacionada)
- **E-13, E-14, E-15** — `00-higiene-repo.md`
- **E-17, E-18** — `referencia/contrato-ddl.md` (sesión REF), encontrados al mapear I1-I9 contra `system_inference.txt`/`prompt_validacion_src.txt`/código
- **E-23, E-24 (hallazgo)** — sesión de cierre O1-b (2026-08-03), corpus real de E-01 corrido a través de `/generate-async`→`/connections`→`/status` (`test_e01_corpus_through_real_async_pipeline_reaches_built`, `test_ktr_build_job_api.py`) — primera vez que el criterio de terminado 3/4 de `10-estabilizar-emision.md` se verifica contra ese camino real, no `build_etl_from_raw()` directo (D64)
- **E-16 (acotado), E-21, E-23, E-24 (cierre)** — D68 (`02-decisiones.md`), sesión O3 (2026-08-04). `_repair_dimension_loader_fields`/`_dimension_repair_context`/`_deterministic_field_mapping` borradas; corpus real de E-01 reverificado en aislamiento contra `apply_dimension_contracts()` (no a través de `/generate-async` — ese re-chequeo queda pendiente, criterio 5 de `30-decision-python-llm.md`)

---

## Nota sobre E-03 (histórica — ver D60 para el cierre)

Se pensó que era el hallazgo más incómodo del lote: que la degradación que O1 quiere generalizar ya existía rota en el camino de repair, aceptando datos alucinados en vez de rechazarlos. Verificado contra código (D60, `02-decisiones.md`): no es así — el gate real (`etl_generator.py:805-815`) rechaza correctamente un `stream_field` inexistente; el test que decía lo contrario reusaba el fixture de su vecino exitoso y nunca ejercía el camino que decía probar. El criterio que igual hacía falta para generalizar la degradación a los 4 sitios de aborto se escribió en D60, sin depender de que este caso puntual resultara roto.
