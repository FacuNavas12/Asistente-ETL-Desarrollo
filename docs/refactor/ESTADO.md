# Estado — Refactor de fragmentación

**Mutable.** Única fuente de estado de fase de todo el refactor — ver regla 1 en `CLAUDE.md`. Se reescribe en el mismo turno en que una fase cambia de estado. Ningún otro archivo repite estado de fase; si necesitan mencionarlo, citan este archivo.

Vocabulario cerrado: `pendiente` / `en curso` / `cerrada` / `bloqueada por <qué>`.

| Fase | Estado | Qué falta exactamente | Detalle en |
|---|---|---|---|
| A0 — Inventario | cerrada (2026-07-25) | — | `docs/auditoria/00-inventario.md` |
| A0.5 — Fallos silenciosos | cerrada (2026-07-25) | — (derivó H29, sin dueño de track) | `docs/auditoria/00b-fallos-silenciosos.md`, D25 |
| A1 — Doc vs. realidad | pendiente | Todo | `03-plan.md` tabla Track A |
| A2 — Cumplimiento por capas | pendiente | Todo | `03-plan.md` tabla Track A |
| A3 — Bordes de entrada | pendiente | Todo | `03-plan.md` tabla Track A |
| A4 — Acoplamiento | pendiente | Todo | `03-plan.md` tabla Track A |
| A5 — Plan de remediación | pendiente | Todo | `03-plan.md` tabla Track A |
| A6 — Consolidar doctrina | en curso | Falta formalizar tras A1-A5 (auditoría completa, todavía pendientes abajo). Doctrina base escrita 2026-07-27 fuera de secuencia (mapa capa↔directorio, regla de migración, R10 forma positiva + R12, ajuste a R11, sobre-especificación de `ports/`, test de arquitectura AST, READMEs por carpeta) y ampliada el mismo día (D27): `backend/app/domain/` existe físicamente por primera vez (`canonical_types.py`), `registry.py` partido en `step_types.py`/`step_emitters.py`, `KNOWN_PDI_STEP_TYPES` borrado (H30), criterio "vocabulario PDI es dominio" fijado en `CLAUDE.md`. Ver `docs/arquitectura-objetivo.md`, `backend/tests/test_architecture_layers.py`, `backend/tests/test_pdi_step_coherence.py`, D27 en `02-decisiones.md` | `docs/arquitectura-objetivo.md`, D27 |
| A7 — Ejecución | pendiente | Todo | `03-plan.md` tabla Track A |
| F1 — Investigar | cerrada (2026-07-22) | — | `03b-reportes.md#reporte-f1` |
| C.2 — Reglas de corte vs. D6-bis | cerrada (2026-07-22) | — | C.2 en `02-decisiones.md` |
| F1.5 — Dominio mínimo del corte | cerrada en código (2026-07-24) | — | H4/H6/H11 en `01-hallazgos.md` |
| F2 — Diseñar el corte | cerrada, aprobada por usuario (2026-07-23, D17) | — | `03b-reportes.md#reporte-f2` |
| F2.5 — Soporte `JobEntryJob` | cerrada en código (2026-07-24) | — | H7 en `01-hallazgos.md` |
| F3 — Implementar el corte | cerrada (2026-07-27) | — | `03b-reportes.md#estado-f3`, D19/D20/D28 en `02-decisiones.md` |
| F4 — Track de errores | en curso | Validador de contrato entre KTR (D23, ejecutado por D38 — nombres; tipos queda gap documentado, no implementado); `validate_business_rules()` removido sin reemplazo en backend, responsabilidad a DDL + Data Validator (PDI) por investigar (D39); progreso observable + checkpoint por etapa del job async (D29-D33, ejecutado); resolución de conexiones sin abortar + mapa por-ETL (D34-D36, ejecutado); criterio determinista SCD1/SCD2 (D37, ejecutado, `domain/scd.py`, verificado por test suite — falta correr un ETL real end-to-end con `uvicorn`+frontend para confirmar que el criterio resuelve en la práctica el tipo de caso que lo motivó: error de KTR y falla de DDL por decisión SCD divergente entre etapas, sin casos formales todavía escritos); H29 recuperación de `table` por contenido (D40, ejecutado, `ktr_builder/validators/` — cerrado parcial, el patrón `if not table: continue` en sí sigue triplicado, ver H29 en `01-hallazgos.md`); **investigación Kettle cerrada 2026-07-30 → D44-D49 escritas + H41-H47 (todos cerrados).** D44 (vocabulario uniforme por rol) ya sin bloqueante de ejecución — C.12 resuelta por D47 (sembrado de fila `tk=0` embebido en el DDL, prescrito por doc oficial Pentaho); D45 (corte visible, con D48: hops cruzados vía tabla de staging, no `Copy rows to result`) sin bloqueo de diseño; D49 (C.11a, `PREFERRED_SCHEMA_NAME` obligatorio) cierra el riesgo real de `search_path`, C.11b (multi-schema completo) queda abierta y separada; D45/D47/D48/D49 diseño cerrado, sin implementar todavía; Fase 2-bis (visibilidad de no-determinismo de `scd_type`, H44) no opcional respecto de D44 — riesgo aceptado explícitamente, ver abajo. **Fase 2 (vocabulario uniforme por rol, D44) + R-K7 (`scd_type==0`, D51) ejecutadas 2026-07-30:** `derive_dimension_step_type()` eliminada, reemplazada por `derive_dimension_loader_step()`/`derive_fact_lookup_step()`/`derive_attribute_update_mode()` (`domain/scd.py`); `dimension_step_policy.py` invierte su auto-fix (`CombinationLookup → DimensionLookup`, sintetizando `fields`/`date_from`/`date_to`/`version_field` desde el contrato) y la rama `fact_lookup` sin versionar pasa de "reporta" a "repara" (R-K2 cierra el residual de D16); guard nuevo (`_scd_zero_calendar_guard`, `etl_generator.py`) rechaza `scd_type=0` fuera del caso calendario, reusando `is_calendar_dimension()` extraído a función nombrada; passes pre-emisión nuevos `check_dimension_lookup_fields`/`check_insert_update_bypass` (`ktr_builder/validators/`); `<update_bypassed>` agregado al emisor de `InsertUpdate` (nunca se emitía, hallazgo lateral); `system_etl.txt` reescrito en las secciones de vocabulario de dimensión (vocabulario uniforme, `modo_por_atributo`, checklist ítem 24 invertido, `19b`/K18/K10 corregidos). Fase 2-bis (mitigaciones 1-4: finding informativo de consecuencia SCD, regla dura de columnas monetarias fuera de `attributes_scd2`, confirmación de usuario, medición de varianza), Fase 2-ter (`FilterRows` de CHECK del DDL, guard de capa staging) y DDL-1 completo (INSERT semilla en el prompt) quedan **fuera de esta sesión, backlog explícito de F4**. **Fase 2-bis, mitigaciones 1+2, ejecutadas 2026-07-30 (D52):** `_scd_consequence_findings()`/`_monetary_scd2_guard()` nuevas en `etl_generator.py`, cableadas en los 4 caminos que tienen `dim_contracts` disponible (flujo síncrono, flujo async, las 2 ramas de `build_etl_from_raw`) — mitigación 3 (confirmación de usuario, superficie de frontend) y mitigación 4 (varianza, ya asignada a Fase 4) siguen backlog explícito, no se tocaron esta sesión. **D47 (DDL-1) ejecutado 2026-07-30:** `prompt_validacion_src.txt` (I8, V5) ahora exige INSERT completo de la fila 'desconocido' — `technical_key=unknown_key_value`, `version_field=1`, `date_from='1900-01-01 00:00:00'`, `date_to='2199-12-31 23:59:59.999'` (centinelas compartidos con DDL-2), y cualquier otra columna NOT NULL sin DEFAULT recibe `'DESCONOCIDO'`/`0` según tipo. Sin test dedicado — texto de prompt LLM, mismo criterio que el resto de `prompt_validacion_src.txt`; gate real queda pendiente de corrida contra el modelo (ver D47 en `02-decisiones.md`, `06-contrato-ddl.md`). **D49 (C.11a, `PREFERRED_SCHEMA_NAME` obligatorio) ejecutado 2026-07-30:** `_DEFAULT_SCHEMA_BY_TYPE` + wiring en `_build_connection()` (`ktr_builder/connection.py`) — ver D49 en `02-decisiones.md`. Suite completa: 646 passed / 54 failed, mismo set preexistente, cero regresión. **D45 Sesión A (puntos 3/4/5/7) ejecutada 2026-07-30, dentro de `fragmentation.py`, sin infraestructura nueva:** C1 con RW desdoblado explícito (steps distintos, ya no por coincidencia con C1-bis — H42), exención por camino dirigido eliminada (`_reaches` borrada, `_connected_components` ahora respeta `hop.enabled`, mismo componente siempre reporta `Finding(severity="error")` — cambio de comportamiento con test invertido: `test_compute_cut_self_lookup_same_component_now_reports_race`), hops que cruzan grupos del corte reportados como `Finding(severity="error")` (hallazgo lateral: con el algoritmo actual solo alcanzable por hop-colgante, no por cruce real, hasta que D48 lo haga alcanzable), inventario de `untouched_comps` como `Finding(severity="info")` cuando hay corte real. Decisión ya registrada para cuando corra la Sesión B: la inferencia por prefijo de tabla (`_STAGING_PREFIXES`/`_DWH_PREFIXES`, hoy en `ktr_builder/connection.py`) se extrae a `domain/` para que la clave `(connection, table)` y el emisor no puedan divergir — ver nota de ejecución bajo D45 en `02-decisiones.md`. Suite completa antes/después: 646→650 passed, mismos 54 fallos preexistentes, cero regresión. **Fase 2-ter (D53), D45 Sesión B (puntos 1/2/6) y D48 — las tres ejecutadas 2026-07-30, en ese orden, cerrando toda la cadena de diseños que quedaba pendiente de `03c-investigacion-vocabulario-dimension-kettle.md`.** De paso, corregidas dos entradas de `02-decisiones.md` cuyo estado en el índice había quedado desalineado del cuerpo/código en sesiones previas (D47 y D49 ya estaban ejecutadas, el índice todavía decía "no implementado"). **D53 (Fase 2-ter) ejecutada:** dos passes pre-emisión nuevos en `ktr_builder/validators/` — `check_constraint_filter_rows` (S-4/H45: exige un `FilterRows` por cada columna con CHECK de rango del DDL DWH que un writer puebla, mínimo aceptable por presencia plana en el mismo KTR, no reachability; cierra media A-5, `value_type` numérico) y `guard_staging_layer` (S-5/H-5: SÍ usa reachability dirigida vía hops — ningún `FilterRows` puede alimentar un writer de tabla `stg_*`, backstop determinista de D42). `domain/table_layer.py` nuevo (`infer_table_layer`, extraído de `connection.py`) — adelanta lo que D45 punto 2 iba a necesitar de todos modos. **D45 Sesión B (puntos 1/2/6) ejecutada — cierra D45 por completo:** punto 1, `domain/sql_resolution.py` (`SqlResolution`/`SqlTableResolver`, Protocol puro) + `services/adapters/sql_table_resolver.py` (sqlglot real) — `build_rw_matrix` acepta `resolve_sql_tables` inyectado, `TableInput`/`StreamLookup`/`ExecSQL` dejan de ser invisibles; `contract_validate.py` pierde su workaround por regex (D23), "un solo resolver para los dos". Punto 2, `fragmentation.MatrixKey = tuple[str,str]` — matriz R/W pasa de `{tabla: ...}` a `{(connection, tabla): ...}`, `_connection_key()` explícita o inferida (mismo criterio de `table_layer.py`). Punto 6, `validate_stage_contract()` nueva (S-13) — el único gap real de los 3 ítems de S-13: un fragmento no puede leer lo que otro fragmento POSTERIOR de la misma etapa escribe. `lineage_builder._TABLE_RE` (regex del diagrama de linaje) queda sin tocar a propósito — no lo necesita el motor de corte, residual conocido, no D45. **D48 ejecutada — con un hallazgo que reabrió D45 punto 4:** al implementar resultó que, con el algoritmo de D45, un hop con extremos conocidos NUNCA cruza un grupo (groups = uniones completas de componentes conexos) — el mecanismo de materialización que D48 diseñó no tenía nada real que materializar hasta reabrir "mismo componente nunca se parte" para el único patrón evidenciado en el corpus (`err1.ktr`/`err2.ktr`): un único writer con TODOS sus readers como ancestros dirigidos suyos (self-lookup `Existe?`/`Filtrar Nuevos`/`Insertar Dim Pais`). Para ese patrón, el componente se parte (writer + descendientes a un grupo posterior) y el hop que queda colgando se materializa vía tabla de staging efímera (`etl_corte_N`, prefijo deliberadamente fuera de `STAGING_TABLE_PREFIXES` para no disparar `guard_staging_layer` de D53). Cualquier otro patrón (2+ writers, o un reader que no es ancestro) sigue exactamente como D45 lo dejó — "revisar a mano", sin partir. Caso general de corte multi-par no construido, sin evidencia de necesidad (D6-bis). Suite completa antes/después de las tres (D53 + D45 Sesión B + D48, en cadena): 636→642 passed, mismos fallos preexistentes (1 en el recorte sin servidor/cuota — `test_ktr_xml_validator.py`, ya roto antes de esta sesión; los de `test_api.py`/`test_ktr_build_job_api.py`, `ConnectionError` por falta de servidor vivo, confirmados sin cambios corriendo la suite completa aparte) — cero regresión, ~15 tests nuevos entre las tres. **Próximo paso recomendado, sin implementar todavía:** con D44-D49/D51-D53 todos ejecutados, lo que queda de F4 es la **Fase 4** de `03c-investigacion-vocabulario-dimension-kettle.md` — corrida real end-to-end contra `uvicorn`+frontend (dos modelos, N corridas por modelo — S-12, mide de paso la varianza de `scd_type` que es la mitigación 4 de Fase 2-bis), más el backlog explícito ya registrado y sin tocar: mitigación 3 de Fase 2-bis (confirmación de usuario del `scd_type` propuesto, superficie de frontend nueva) y C.11b (`schema` obligatorio end-to-end, multi-schema completo). Ninguno de los tres depende de código nuevo del motor de corte — Fase 3/D45/D48 están completas. Numeración: el código de la sesión de Fase 0 usa `D50` en comentarios donde este documento dice D46 (drift preexistente, no de esta sesión) — la entrada nueva se numera D51 para no agravarlo, ver nota en D51. **Fase 0 (red de seguridad, D46) ejecutada 2026-07-30:** `error_catalog_checks.py` (V4/V5/V6/V7/V8/V11/V13) cableada al final de `build_ktr` con `ERROR_CATALOG_PREFIX`, golden negativo contra fixtures reales de `Base_01` saneadas (`backend/tests/fixtures/golden_run_base_01/`, password Kettle real removido) — pasa en cero findings, `v8` no necesitó corrección de código (ya estaba bien, verificado); `Finding.severity` revivida vía `split_findings_by_severity()`/`PRE_EMIT_ERROR_PREFIX` (`validators/base.py`) en `_recover_table_keys` y en el pass best-effort de `build.py`, sin cambiar el tipo de retorno `list[str]` en los ~7 call sites existentes; `compute_cut`/`split_ktr_by_cut` (`fragmentation.py`) devuelven `list[Finding]` (V2 severity="warning", "sin relación segura"/"ciclo" severity="error"); `EtlDetail.jsx` exige `window.confirm` antes de descargar el .ktr si hay `Validacion(tipo="error")` (S-14, no bloquea la descarga, D15 intacto); marker de debug `DIMLOOKUP_MARKER` borrado (`steps/lookups.py`). Suite backend completa corrida antes/después (605 passed / 54 failed en ambas corridas, idéntico set de fallos): los 54 son preexistentes y sin relación con este cambio (36 `test_api.py`/6 `test_ktr_build_job_api.py` sin servidor vivo, 11 `test_structured_outputs.py` por cuota Gemini agotada, 1 `test_ktr_xml_validator.py` ya roto antes de esta sesión) — cero regresión nueva, +2 tests (golden negativo S-1/S-3 en `test_error_catalog_checks.py`). Pendiente de Fase 0: nada — los 5 ítems de la red de seguridad están cerrados en código. **nuevo track abierto — `06-contrato-ddl.md`**, consolida deuda de contrato DDL empezando por D47 (DDL-1) más los puntos que el usuario está señalando aparte; `03-plan.md` Track F y `01b/02b-*-cerradas.md` quedan sin tocar en esta sesión (fuera de presupuesto de lectura); ver `04-verificacion.md` para lo pendiente de re-correr. **Corrida real (D54, 2026-07-31) — parcial:** de los 2 modelos pedidos por S-12, Haiku dio 0 corridas completas (falla en inferencia de estructuras, `fase4_manual/haiku/Error al Inferir.txt` — garantía de structured output más débil en Anthropic que en Gemini, más visible en modelo chico + schema grande) y Sonnet dio 1 corrida completa (`fase4_manual/sonnet/`). El corte STG→DWH sí dio 2 archivos en esa corrida. Análisis de esa corrida encontró H51 (loader de dimensión con `update="N"`, equivalente a solo-lectura, pasa sin detectar — ni `enforce_dimension_step_policy` ni `check_dimension_lookup_fields` verifican ese flag para rol loader). Criterio de cierre real de Fase 4 (2+ modelos × N corridas c/u, S-12) **no se cumple todavía** — falta al menos 1 corrida más de Sonnet y retomar Haiku (caso más chico, o esperar crédito Gemini) | D21-D23, D29-D54 en `02-decisiones.md`, H51 en `01-hallazgos.md`, `03c-investigacion-vocabulario-dimension-kettle.md`, `investigacion-kettle-RK1-RK6.md`, `investigacion-pentaho-C10-C11-C12.md`, `06-contrato-ddl.md` |
| F5 — Limpieza de bajo costo | cerrada en código (2026-07-25) | — | H12 en `01-hallazgos.md` |
